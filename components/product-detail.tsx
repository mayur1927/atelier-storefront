"use client";

import Link from "next/link";
import { Heart, ShoppingBag, Star } from "lucide-react";
import { useState } from "react";
import { getProductVariant, getProductVariants, type Product } from "@/lib/mockData";
import { useStore } from "@/context/store-context";
import { Breadcrumbs, QuantityPicker } from "@/components/shared";

export function ProductDetail({ product }: { product: Product }) {
  const [size, setSize] = useState(product.sizes[0]);
  const [color, setColor] = useState(product.colors[0]);
  const [quantity, setQuantity] = useState(1);
  const [activeTab, setActiveTab] = useState("Description");
  const { addToCart, toggleWishlist, wishlistIds } = useStore();
  const inWishlist = wishlistIds.includes(product.id);
  const variants = getProductVariants(product);
  const selectedVariant = getProductVariant(product, color);
  const add = () => { for (let i = 0; i < quantity; i += 1) addToCart(product, { size, color, image: selectedVariant.image }); };
  return <div className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: product.category, href: `/category/${product.category.toLowerCase()}` }, { label: product.name }]} /><div className="mt-6 grid gap-8 lg:grid-cols-[1.12fr_0.88fr] lg:gap-14"><div className="grid gap-3 sm:grid-cols-[90px_1fr]"><div className="order-2 flex gap-3 sm:order-1 sm:flex-col"><button aria-label={`${color} product image`} aria-pressed={true} className="aspect-square w-16 overflow-hidden rounded-lg border-2 border-zinc-950 sm:w-auto"><img src={selectedVariant.image} alt="" className="h-full w-full object-cover grayscale" /></button><button className="aspect-square w-16 rounded-lg bg-zinc-100 sm:w-auto" aria-label="Product alternate angle" /></div><div className="aspect-[4/5] overflow-hidden rounded-2xl bg-zinc-100"><img key={selectedVariant.image} src={selectedVariant.image} alt={`${product.name} in ${color}`} className="h-full w-full object-cover grayscale" /></div></div><div className="lg:pt-3"><p className="text-xs font-bold uppercase tracking-[0.15em] text-zinc-400">{product.brand} / {product.category}</p><div className="mt-2 flex items-start justify-between gap-4"><h1 className="text-3xl font-black tracking-[-0.055em] sm:text-4xl">{product.name}</h1><button onClick={() => toggleWishlist(product.id)} className="rounded-full border border-zinc-200 p-3" aria-label="Toggle wishlist"><Heart size={19} className={inWishlist ? "fill-zinc-950" : ""} /></button></div><div className="mt-4 flex items-center gap-2"><Star size={16} className="fill-zinc-950" /><span className="font-semibold">{product.rating.toFixed(1)}</span><span className="text-sm text-zinc-500">({product.reviewCount} reviews)</span></div><p className="mt-5 text-2xl font-bold">${product.price.toFixed(2)}</p><p className="mt-1 text-xs text-zinc-500">Inclusive of taxes. Shipping calculated at checkout.</p><div className="mt-7"><div className="flex justify-between text-sm"><span className="font-bold">Color</span><span className="text-zinc-500">{color}</span></div><div className="mt-3 flex flex-wrap gap-2">{variants.map((variant) => <button key={variant.color} onClick={() => setColor(variant.color)} className={`rounded-full border px-4 py-2 text-sm ${color === variant.color ? "border-zinc-950 bg-zinc-950 text-white" : "border-zinc-200"}`}>{variant.color}</button>)}</div></div><div className="mt-6"><p className="text-sm font-bold">Size</p><div className="mt-3 flex flex-wrap gap-2">{product.sizes.map((option) => <button key={option} onClick={() => setSize(option)} className={`min-w-11 rounded-md border px-3 py-2 text-sm ${size === option ? "border-zinc-950 bg-zinc-950 text-white" : "border-zinc-200"}`}>{option}</button>)}</div></div><div className="mt-6"><p className="mb-3 text-sm font-bold">Quantity</p><QuantityPicker quantity={quantity} onChange={setQuantity} /></div><div className="mt-7 grid gap-3 sm:grid-cols-[1fr_auto]"><button onClick={add} className="flex items-center justify-center gap-2 rounded-xl bg-zinc-950 px-5 py-4 text-xs font-bold tracking-[0.13em] text-white hover:bg-zinc-700"><ShoppingBag size={17} /> ADD TO CART</button><button onClick={() => { add(); window.location.assign("/checkout"); }} className="rounded-xl border border-zinc-950 px-6 py-4 text-xs font-bold tracking-[0.13em] hover:bg-zinc-100">BUY NOW</button></div><div className="mt-7 grid grid-cols-3 gap-3 border-y border-zinc-200 py-5 text-center text-xs text-zinc-500"><span>Free shipping<br /><b className="text-zinc-900">over $75</b></span><span>Easy returns<br /><b className="text-zinc-900">30 days</b></span><span>Secure checkout<br /><b className="text-zinc-900">encrypted</b></span></div></div></div><div className="mt-14 border-t border-zinc-200"><div className="flex overflow-auto">{["Description", "Reviews", "Shipping & Returns"].map((tab) => <button onClick={() => setActiveTab(tab)} key={tab} className={`whitespace-nowrap border-b-2 px-4 py-4 text-sm font-bold ${activeTab === tab ? "border-zinc-950" : "border-transparent text-zinc-400"}`}>{tab}</button>)}</div><div className="max-w-2xl py-6 text-sm leading-6 text-zinc-600">{activeTab === "Description" ? <><p>{product.description}</p><ul className="mt-3 list-disc pl-5"><li>Designed for easy, everyday wear.</li><li>Available in multiple colors and sizes.</li><li>Thoughtfully made in limited quantities.</li></ul></> : activeTab === "Reviews" ? <p>Customers love the fit, quality, and understated details. This product is rated {product.rating.toFixed(1)} out of 5 from {product.reviewCount} verified reviews.</p> : <p>Orders over $75 ship free. Returns are accepted within 30 days of delivery in original condition. <Link href="/shipping" className="underline">Read the full policy</Link>.</p>}</div></div></div>;
}
